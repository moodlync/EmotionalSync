// MoodLync Theme Settings
// These settings control theme-related behaviors throughout the application

// When true, forces auth and welcome pages to always use light mode regardless of system preference
export const AUTH_PAGES_FORCE_LIGHT_MODE = true;

// Controls user state persistence behavior
// 'always' - Always persist user state across browser sessions
// 'never' - Never persist user state across browser sessions
// 'conditional' - Only persist user state when the "Remember Me" checkbox is selected
export const PERSIST_USER_STATE = 'conditional';